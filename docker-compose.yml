name: aeie-club
version: '0.0000'

services:

  traefik:
    image: "traefik:v3.5"
    container_name: "traefik"
    command:
      #- "--log.level=DEBUG"
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entryPoints.web.address=:80"
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"

  whoami:
    image: "traefik/whoami"
    container_name: "simple-service"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.whoami.rule=Host(`whoami.localhost`)"
      - "traefik.http.routers.whoami.entrypoints=web"
  backend:
   image: "sumanhazra914/aeie_club_server:latest"
   environment:
    - DB_URL=mongodb+srv://sumanhazra368:tDnqBItGtxlrb0TL@aeiecluster0.lt2akvi.mongodb.net/?retryWrites=true&w=majority&appName=AEIEcluster0
    - IMAGEKIT_PUBLIC_KEY=public_IdL2aCe8j0dEwXVT5LNygLDQ3XE=
    - IMAGEKIT_PRIVATE_KEY=private_pZmWx4fmNC99DJIn7OFyv854ud0=
    - IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/AEIE
    - JWT_SECRET=your_jwt_secret_key

   deploy:
      mode: replicated
      replicas: 2
   labels:
    - "traefik.enable=true"
    - "traefik.http.routers.backend.rule=Host(`backend.localhost`)"
    - "traefik.http.routers.backend.entrypoints=web"
    