FROM haproxy:latest

# Copy HAProxy configuration
COPY haproxy.cfg /usr/local/etc/haproxy/haproxy.cfg

# Expose ports 80 (main traffic) and 8404 (stats)
EXPOSE 3000

# HAProxy runs automatically with the default entrypoint