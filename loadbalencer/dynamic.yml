
# dynamic.yml
http:
  routers:
    api-router:
      rule: "PathPrefix(`/api`)"
      service: "backend-api"
      entryPoints:
        - web

  services:
    backend-api:
      loadBalancer:
        servers:
          - url: "https://54.188.71.94:3000"
          - url: "https://52.13.128.108:3000"

   